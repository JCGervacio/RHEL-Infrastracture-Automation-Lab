---

- name: Ensure developer group exists
  ansible.builtin.group:
    name: developers
    state: present

- name: Create application users
  ansible.builtin.user:
    name: "{{ item }}"
    groups: developers
    append: true
    shell: /bin/bash
    state: present
  loop: "{{ user_mgmt_users }}"
